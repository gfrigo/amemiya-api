# Example secure mosquitto configuration (template)
# This file is a template. Replace paths with actual certs and password files before enabling in production.

persistence true
persistence_location /mosquitto/data/
log_dest stdout

# TLS listener (MQTT over TLS)
# listener 8883
# protocol mqtt
# cafile /mosquitto/config/certs/ca.crt
# certfile /mosquitto/config/certs/server.crt
# keyfile /mosquitto/config/certs/server.key

# WebSocket listener with TLS
# listener 8081
# protocol websockets
# cafile /mosquitto/config/certs/ca.crt
# certfile /mosquitto/config/certs/server.crt
# keyfile /mosquitto/config/certs/server.key

# Require username/password (example)
# allow_anonymous false
# password_file /mosquitto/config/passwords

# For production consider using EMQX or another broker with JWT/HTTP auth plugin for
# integrating the API authentication (JWT) directly.
